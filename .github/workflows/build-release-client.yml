name: Build and Release Client

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

permissions:
  contents: write

jobs:
  # ---------- Windows build ----------
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.13'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build executable
        run: |
          pyinstaller main.py --onefile --noconsole --name Bazaar Oracle `
          --icon "assets\brand_icon.ico" --add-data "tools\tesseract;tesseract"

      - name: Create / update release
        uses: softprops/action-gh-release@v1
        with:
          files: dist/main.exe
          name: Release ${{ github.ref_name }}
          tag_name: ${{ github.ref_name }}
          draft: false
          prerelease: false

  # ---------- macOS build ----------
  # build-mac:
  #   runs-on: macos-latest
  #   steps:
  #     - uses: actions/checkout@v3

  #     - name: Set up Python
  #       uses: actions/setup-python@v4
  #       with:
  #         python-version: '3.13'

  #     - name: Install dependencies
  #       run: |
  #         python -m pip install --upgrade pip
  #         pip install -r requirements.txt

  #     - name: Build executable
  #       run: |  
  #         pyinstaller --onefile main.py --no-console \
  #           --add-data "tools/tesseract:tesseract" \
  #           --add-data "data:data"

  #     - name: Create / update release
  #       uses: softprops/action-gh-release@v1
  #       with:
  #         files: dist/main
  #         name: Release ${{ github.ref_name }}
  #         tag_name: ${{ github.ref_name }}
  #         draft: false
  #         prerelease: false
